spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/boot_camp_rating_net # ${datasource.url}
    username: root # ${datasource.username}
    password: 1234 # ${datasource.password}

